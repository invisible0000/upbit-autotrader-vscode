# ═══════════════════════════════════════════════════════════════
# 🎯 META_PYRAMID_TARGET (피라미딩) - 플레이스홀더 텍스트
# ═══════════════════════════════════════════════════════════════
# 대응 테이블: tv_placeholder_texts
# 지표 ID: META_PYRAMID_TARGET
# ═══════════════════════════════════════════════════════════════

# 매개변수별 플레이스홀더 텍스트
placeholders:
  # ─────────────────────────────────────────────────────────────
  # 📊 추적 기간 (Tracking Period)
  # ─────────────────────────────────────────────────────────────
  META_PYRAMID_TARGET_tracking_period:
    variable_id: "META_PYRAMID_TARGET"
    parameter_name: "tracking_period"
    placeholder_text_ko: "예: 14 (14일 추적 기간)"
    placeholder_text_en: "e.g., 14 (14-day tracking period)"
    example_values: ["5", "10", "14", "20", "50"]

  # ─────────────────────────────────────────────────────────────
  # 📈 진입 비율 (Entry Ratio)
  # ─────────────────────────────────────────────────────────────
  META_PYRAMID_TARGET_entry_ratio:
    variable_id: "META_PYRAMID_TARGET"
    parameter_name: "entry_ratio"
    placeholder_text_ko: "예: 0.03 (3% 진입 비율)"
    placeholder_text_en: "e.g., 0.03 (3% entry ratio)"
    example_values: ["0.01", "0.03", "0.05", "0.07"]

  # ─────────────────────────────────────────────────────────────
  # 🔢 최대 횟수 (Max Count)
  # ─────────────────────────────────────────────────────────────
  META_PYRAMID_TARGET_max_pyramid_count:
    variable_id: "META_PYRAMID_TARGET"
    parameter_name: "max_pyramid_count"
    placeholder_text_ko: "예: 3 (최대 3회)"
    placeholder_text_en: "e.g., 3 (max 3 times)"
    example_values: ["1", "2", "3", "5"]

  # ─────────────────────────────────────────────────────────────
  # ⏰ 타임프레임 (Timeframe)
  # ─────────────────────────────────────────────────────────────
  META_PYRAMID_TARGET_timeframe:
    variable_id: "META_PYRAMID_TARGET"
    parameter_name: "timeframe"
    placeholder_text_ko: "전략 기본값 사용"
    placeholder_text_en: "Use strategy default"
    example_values: ["position_follow", "1h", "4h", "1d"]
