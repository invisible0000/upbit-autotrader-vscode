# ═══════════════════════════════════════════════════════════════
# 🎯 META_TRAILING_STOP (트레일링 스탑) - 플레이스홀더 텍스트
# ═══════════════════════════════════════════════════════════════
# 대응 테이블: tv_placeholder_texts
# 지표 ID: META_TRAILING_STOP
# ═══════════════════════════════════════════════════════════════

# 매개변수별 플레이스홀더 텍스트
placeholders:
  # ─────────────────────────────────────────────────────────────
  # 📊 추적 기간 (Tracking Period)
  # ─────────────────────────────────────────────────────────────
  META_TRAILING_STOP_tracking_period:
    variable_id: "META_TRAILING_STOP"
    parameter_name: "tracking_period"
    placeholder_text_ko: "예: 14 (14일 추적 기간)"
    placeholder_text_en: "e.g., 14 (14-day tracking period)"
    example_values: ["5", "10", "14", "20", "50"]

  # ─────────────────────────────────────────────────────────────
  # 📈 임계값 비율 (Threshold Ratio)
  # ─────────────────────────────────────────────────────────────
  META_TRAILING_STOP_threshold_ratio:
    variable_id: "META_TRAILING_STOP"
    parameter_name: "threshold_ratio"
    placeholder_text_ko: "예: 0.02 (2% 임계값)"
    placeholder_text_en: "e.g., 0.02 (2% threshold)"
    example_values: ["0.01", "0.02", "0.03", "0.05"]

  # ─────────────────────────────────────────────────────────────
  # ⏰ 타임프레임 (Timeframe)
  # ─────────────────────────────────────────────────────────────
  META_TRAILING_STOP_timeframe:
    variable_id: "META_TRAILING_STOP"
    parameter_name: "timeframe"
    placeholder_text_ko: "전략 기본값 사용"
    placeholder_text_en: "Use strategy default"
    example_values: ["position_follow", "1h", "4h", "1d"]
